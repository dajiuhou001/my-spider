name: Auto Spider Bot

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  run_spider:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: |
          npm install playwright axios cheerio

      - name: Install Browser
        run: npx playwright install chromium --with-deps

      - name: Create Script File
        run: |
          cat << 'EOF' > spider_script.js
          const { chromium } = require('playwright');
          const axios = require('axios');
          const cheerio = require('cheerio');

          async function run() {
            console.log('--- 启动浏览器引擎 (绕过五秒盾) ---');
            const browser = await chromium.launch({ headless: true });
            const context = await browser.newContext({
              userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/143.0.0.0 Safari/537.36 Edg/143.0.0.0'
            });
            const page = await context.newPage();

            try {
              // 设置 Cookie
              await context.addCookies([{
                name: 'cf_clearance',
                value: 'hGqOKGlLUQOdBIMiOFvKOcW3kPBkvDPr5XgRTJMCQ_U-1767995732-1.2.1.1-zMyCvc_wL_LFCvoBTlaImCPAkZMwlUBilOFVRBjpccUJliQBGGriP9Ma6y_iqalTDGI4A8Q31RYfIWvaSplc4qIoKTmE._DLgYqMmbJccjcrm_TDsoCAr2oIOnzgh2cRmvo0xzROXn40cD.OyhtOvWYXDK49gq2GfliqGO8t85HGDhdeDd7vybMQV3SLWSYG6mhoR6KHPs9SgUkrk4aZEr9vaNrMyYYUFMIBNqpGHOo',
                domain: 's.url99.me',
                path: '/'
              }]);

              console.log('正在打开页面并等待 Cloudflare 验证...');
              await page.goto('https://s.url99.me/7cnr519a', { waitUntil: 'networkidle', timeout: 60000 });

              // 等待表格加载出来
              await page.waitForSelector('tr.el-table__row', { timeout: 10000 }).catch(() => console.log('表格加载超时，可能仍被拦截'));

              const html = await page.content();
              const $ = cheerio.load(html);
              const list = [];
              
              $('tr.el-table__row').each((i, el) => {
                const number = $(el).find('.el-table_1_column_1 .cell').text().trim();
                const status = $(el).find('.el-table_1_column_3 .cell span').text().trim();
                if (status.includes('在线') && number) {
                  list.push({ number, status: '在线' });
                }
              });

              console.log('抓取完成，在线号码:', list.length);

              if (list.length >= 0) {
                console.log('正在推送至宝塔...');
                await axios.post('http://204.141.218.119:4000/api/update', { data: list }, { timeout: 10000 });
                console.log('同步成功！');
              }

            } catch (e) {
              console.error('任务失败:', e.message);
              process.exit(1);
            } finally {
              await browser.close();
            }
          }
          run();
          EOF

      - name: Execute Script
        run: node spider_script.js
